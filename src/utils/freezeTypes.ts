export const popupType = Object.freeze({
  DIARY_EDITION_OR_VIEWER: 'DIARY_EDITION_OR_VIEWER',
  TAG_EDITION: 'TAG_EDITION',
});

export const floatingPopupType = Object.freeze({
  TAG_MANAGEMENT_SELECTION: 'TAG_MANAGEMENT_SELECTION',
});

export const diaryPopupModeType = Object.freeze({
  CREATION: 'CREATION',
  UPDATION: 'UPDATION',
  VIEWER: 'VIEWER',
});

export const tagEditionPopupModeType = Object.freeze({
  CREATION: 'CREATION',
  UPDATION: 'UPDATION',
});